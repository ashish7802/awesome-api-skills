version: '3'
services:
  traefik:
    image: traefik:v2.10
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
  api:
    image: my-api
    labels:
      - "traefik.http.routers.api.rule=Host(`api.local`)"